@using EntityLayer.Concrete;
@model List<Transaction>
@{
    ViewData["Title"] = "SaleDetails";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int count = 0;
}


<h3 class="container"> Personel Satış Detayları</h3>
<br />
<ol class="breadcrumb mb-4">
    <h3 class="breadcrumb-item active container">Personel Adı Soyadı: @ViewBag.staff </h3>
</ol>


<table class="table table-hover">

    <tr>
        <th>#</th>
        <th>Ürün</th>
        <th>Cari Ad Soyad </th>
        <th>Fiyat  </th>
        <th>Adet  </th>
        <th>Toplam  </th>
        <th>Tarih  </th>
        <th>Yazdır  </th>

    </tr>

    @foreach (var item in Model)
    {
        count++;

        <tr>
            <th>@count</th>
            <th>@item.Product.ProductName</th>
            <td>  @item.Current.Name  @item.Current.Surname</td>
            <td>@item.Price</td>
            <td>@item.Number</td>
            <td>@item.TotalPrice</td>
            <td>@item.Date.ToShortDateString()</td>
            <td><a href="#" class="btn btn-dark">Yazdır</a></td>
            
        </tr>
    }
</table>
